# TODO — working MVP

Updated: 2026-09-06. Strategy, code evidence, scope and acceptance scenarios: [PLAN.md](PLAN.md).

This replaces the March security-first two-week schedule with a dependency-ordered MVP backlog. Existing components are acknowledged below; a checked component does not imply an integrated feature. All unfinished MVP work is unchecked. Longer-term ideas are retained in the deferred backlog.

## Verified starting point (historical assessment, before M0)

The warnings/failures below record the initial assessment, not the current state.
See completed M0 items for fixes and verification evidence.

- [x] Inspect the UI, gateway, manager, worker, storage, serving, compiler and root deployment wiring.
- [x] Confirm auth/site/version handlers, dashboard/chat/profile/reset pages and Docker configuration exist.
- [x] Run available untagged Go tests in all six modules: passed; compiler has no test files and some worker/serving tests are skipped.
- [x] Compile the bundled sample with the starter theme: home/about/contact pages and assets generated.
- [x] Run UI production build: passed with a CSS unexpected-`}` warning.
- [x] Run UI lint: baseline is **17 errors and 1 warning**, not passing.
- [x] Validate Compose syntax and inspect project containers: config valid with missing OAuth/LLM variables; no project containers running.
- [x] Create an evidence-backed MVP plan with milestone exit criteria.
- [ ] Demonstrate a complete real AI build, preview, publication and rollback. **Not currently working or verified.**

## M0 — Reproducible development and checks

Exit: a fresh checkout builds, initializes and restarts predictably; baseline checks run in CI.

Status: all seven implementation items are complete and locally verified. CI is
configured and validated; its first hosted run remains pending a push (not requested).

- [x] M0.1 Document compatible Go/Node/Docker prerequisites from module files, lockfiles and Docker images; verify clean UI dependency installation and all selected image builds. Implementation: `8f404df`; clean local/container `npm ci` and UI build, all seven selected images passed.
- [x] M0.2 Fix UI lint errors in auth state initialization, socket reconnection, typed error handling and component exports; resolve the hook dependency warning and CSS build warning. Implementation: `b32037f`; local and container lint (`--max-warnings=0`) and build passed without warnings.
- [x] M0.3 Standardize unit/integration commands and prerequisites. Include manager integration tests in the root target; provision a dedicated test DB, Redis and the actual services needed by HTTP integration tests. Implementation: `828d8cf`; six-module package checks and isolated race-enabled gateway/manager/storage suites passed, including five formerly skipped CLI database tests.
- [x] M0.4 Use one migration source with transactional version tracking; reconcile embedded gateway migrations, SQL files and the stub DB helper. Test empty DB startup and restart/upgrade of an existing DB. Implementation: `946554c`; empty/restart, tracked/manual legacy upgrade, rollback/retry, invalid-data preservation, concurrent starts and future-version rejection passed against PostgreSQL; gateway image and integration suites passed.
- [x] M0.5 Simplify the single-host Compose setup to named-volume filesystem storage; remove the unnecessary privileged NFS service from this path. Separate host-port overrides from stable internal ports/healthchecks. Implementation: `eb6e0ec`; fresh-stack and container-recreation smoke checks passed with random host ports, persisted auth/site/artifact data, UI assets and nginx validation. Fixed the discovered IPv6-localhost themes health failure.
- [x] M0.6 Add CI for Go package tests, UI build/lint and compiler fixture build. Record skipped tests explicitly; add integration/browser checks as M1–M3 land. Implementation: `1cc5cd9`; four CI jobs include isolated integration and startup/recreation checks. Compiler fixture, actionlint and local baseline checks passed; five remaining skips inventoried in `docs/TESTING.md`. Hosted CI awaits a push.
- [x] M0.7 Update README startup/status claims and distinguish seeded hosting diagnostics from the full MVP smoke test. Implementation: `fca46ca`; commands/ports and data-preservation guidance reviewed against Compose/Make, root and overlay config validation passed, local documentation links and Make dry-runs passed. README explicitly describes M0 as a baseline, not a working AI MVP.

## M1 — Contracts, initial source and artifacts

Depends on M0. Exit: a deterministic build traverses actual gateway/manager/storage/serving interfaces and produces hosted HTML without manual file seeding.

- [x] M1.1 Define canonical job request/result/status schemas: `prompt`, `source_version`, `target_version`, `job_id`, errors and owner/site association. Align gateway, manager, worker and UI with HTTP contract tests. Implementation: `ef544f9`; [canonical contract](docs/JOB_CONTRACT.md), owner-derived requests, validated identity/status callbacks, structured manager errors and UI runtime parsers. Go package/race tests, isolated gateway/manager and worker callback round-trips (including clarification/ownership/conflict), 11 UI contract tests, zero-warning lint/build, affected image builds and actionlint passed. Worker runner source is now tracked after correcting its ignore rule. Durable mapping remains M1.2; no real AI/publish flow claimed.
- [x] M1.2 Persist the job-to-target-version mapping before dispatch; stop using job IDs as artifact IDs. Handle failed enqueue/version DB writes and duplicate submissions explicitly. Implementation: `d86f737`; migration 007 atomically reserves submission + version, gateway sends persisted independent IDs, manager deduplicates explicit IDs, and UI retains retry keys. Package/race tests, two isolated integration runs, 16 UI tests, zero-warning lint/build, image builds and startup/recreation smoke passed. Tests cover concurrent duplicates, rollback, upgrade/reconnect, lost response, rejection/outcome-write failure and unknown manager state. [Retry/uncertainty boundaries](docs/BUILD_SUBMISSIONS.md): ambiguous submissions reconcile by GET only; automatic recovery remains M2, live status synchronization remains M3.
- [x] M1.3 Align all three storage clients with server routes; stream raw gzip bytes or implement one explicit multipart decoder. Verify uploaded/downloaded bytes and archive extraction match. Implementation: `0fa1044`; [artifact transport contract](docs/ARTIFACT_TRANSPORT.md), canonical GET/PUT routes, raw gzip streaming, multipart rejection, interrupted-download preservation and gzip trailer validation. Six-module package tests, two five-module race-enabled integration runs, affected image builds and startup/recreation smoke passed. One worker-packed text/binary fixture round-trips through real storage and all three clients with exact-byte/extracted-file comparisons. Manifest/private logs remain M1.4; immutability/deletion remain M1.5; no nginx publication or full AI build claimed.
- [x] M1.4 Implement and test manifest and private log persistence using agreed endpoints. Make complete versions visible only after required artifact/metadata writes succeed. Implementation: `73d3635`; [metadata contract](docs/VERSION_METADATA.md), private JSON sidecars, manifest-last commit after artifact/log persistence, completed-only listings and worker callback gating. Package and storage/worker race tests, two isolated integration runs, affected images and metadata persistence across container recreation passed. Failed writes, hidden partial/legacy versions, retry, sorting, restart and metadata validation are covered. Legacy files are preserved, not automatically certified complete. Immutable writes/deletion remain M1.5; internal-service authentication and redaction remain M4.
- [x] M1.5 Enforce immutable version writes and safe temporary-file/atomic commit behavior. Implement version deletion, including metadata cleanup and active-version protection, or disable deletion in MVP UI/API. Implementation: `0cbeaa5`; [immutable-write contract](docs/IMMUTABLE_VERSIONS.md), unique temporary files, no-replace publication, checked file/directory sync, identical retries and `409` conflicts. Deletion is disabled: UI action/client removed, gateway returns `501` without storage/DB calls, storage remains `405`. Package/race tests, competing subprocesses, isolated integration, UI contracts/zero-warning lint/build and startup/recreation preservation checks passed. Existing versions are retained; cleanup/retention and future coordinated deletion are not implemented.
- [x] M1.6 Bootstrap each new site with valid `content/site.json` and home content; explicitly map the UI template to bundled `starter`. Surface initialization failures and make retries safe. Implementation: `a43d0ac`; [bootstrap contract](docs/SITE_BOOTSTRAP.md), migration 008 reserves exact initial archive/metadata bytes with the site, immutable uploads precede atomic DB readiness, and same-owner/domain retries reuse the reservation. UI selects starter and offers Resume Setup; pending builds/enablement are blocked. Package/race tests, two isolated integration runs, compiler seed compatibility, UI contracts/zero-warning lint/build and startup/recreation passed. Legacy sites are preserved; initial source is not compiled/published output.
- [x] M1.7 Define and validate the archive layout (`content/`, `public/`, manifest); keep execution instructions/logs/secrets out of public output and preserve source for future edits. Implementation: `1d9b5bf`; [archive contract](docs/ARCHIVE_LAYOUT.md), versioned layout metadata, allowlisted packing, shared worker/serving validation policy, bounded staged extraction and public-only deployment. Source survives a second edit; runtime files and known private filenames are excluded/rejected. Revision-2 bootstrap adds a layout manifest while persisted revision-1 source remains usable. Package/race tests, two isolated integration runs, compiler fixture, affected image builds and startup/recreation passed; retries/conflicts, hostile paths/links, expansion limits and private-path HTTP 404s are covered. This is structural validation, not arbitrary secret scanning or proof of trusted compilation; M2/M4 retain those gates.
- [x] M1.8 Add compiler fixtures for starter rendering, home discovery, MDX/component errors, navigation, assets and malformed configuration. Test traversal, escaping and symlink boundaries. Implementation: `6bc86f1`; [compiler contract](docs/COMPILER_CONTRACT.md), real starter fixtures and CLI process tests. Fixes include MDX discovery, duplicate routes, title/nav/TOC consistency, component diagnostics, deterministic/validated tokens, traversal/symlink rejection and fresh staged output. Corrected the ignore rule that excluded CLI source. Six-module package tests, compiler race tests (86.3% internal statement coverage), vet, starter/bootstrap CLI smoke and fresh staged-checkout verification passed. Output must be absent/empty; concurrent hostile filesystem mutation, worker integration and production HTML policy remain later gates.
- [x] M1.9 Fix serving payloads (`version` versus `version_id`) and normalize gateway version responses to UI fields, statuses and timestamps. Align the chat route's `siteId` parameter with the page's `fqdn` lookup. Implementation: `8609d32`; [version API contract](docs/VERSION_API.md), canonical serving bodies, explicit committed-artifact summaries with UTC timestamps, safe pagination, UI response validation and shared FQDN routing. Six-module package tests, gateway race tests, isolated HTTP integration, 20 UI contracts, zero-warning lint/build and affected-image startup/recreation passed. Pending/failed job history, UI preview activation, hosting URLs and serving/DB consistency remain M3; committed source is not automatically publishable.
- [x] M1.10 Add a deterministic executor fixture used only for tests; round-trip a compiled artifact through the real service routes. Implementation: `92ea617`; [deterministic acceptance contract](docs/DETERMINISTIC_ROUNDTRIP.md). HTTP registration/bootstrap/build, actual manager snapshot and worker pipeline, real compiler, immutable storage and gateway deployment reach serving/nginx without manual public-file seeding or direct DB edits. Hosted HTML/assets match archive bytes; private paths return 404; conflicting writes and rejected source-only deployment preserve stored/live output. Six-module package tests and five-module race-enabled isolated integration passed; five existing skips remain. M1's deterministic contract exit is verified. The harness now uses M2.1's integration-only manual launch bridge and co-locates nginx only in tests; production AI execution/compilation and deployment topology remain M2/M3. At handoff, M2.1 was next.

## M2 — Real builds and recoverable jobs

Depends on M1. Exit: a real request changes site content; successive edits preserve changes; failures terminate visibly and leave live content intact.

- [x] M2.1 Implement the Docker spawner and configure the intended network, explicit worker image tag, work directory, storage/manager endpoints and scoped credentials. Remove ambiguity with the manager's separate mock worker image. Distinguish definitely-not-started from ambiguous container create/start errors before reporting a definitive submission rejection. Implementation: `d9bb650`; [Docker launch contract](docs/DOCKER_SPAWNER.md). Unix-socket create/start uses `pagewright-worker:m2.1`, project network, private `/work`, explicit endpoints and an environment allowlist with a separate worker provider key. Only positive no-start evidence produces `spawn_failed`; uncertain outcomes retain the reservation/lock and retries do not respawn or overwrite fast callbacks. Package baseline, repeated manager race/vet checks, real-Docker fixture launch/network/workspace/isolation/conflict/missing-image acceptance, full isolated integration, worker image build and startup/recreation passed. CI includes daemon acceptance; five existing skips remain. Manager socket access is host-level authority; per-job authorization, lease renewal/recovery, real AI/compiler integration and container retention policy remain later gates. Next: M2.2.
- [x] M2.2 Replace request-handler spawning with bounded queue dispatch; acknowledge jobs durably and recover interrupted dispatch without duplicate execution. Implementation: `5da77ad`; [dispatch contract and upgrade caution](docs/QUEUE_DISPATCH.md). Atomic `201 pending` admission, per-site queued-job guard, shared active-job cap (default 4), Redis-clock token-fenced pre-intent claim recovery, irreversible launch intent and terminal capacity release replace HTTP spawning. Lost/ambiguous intent is never replayed and holds capacity pending M2.8/M2.9 reconciliation. Minimum Redis AOF/volume persistence brought forward from M2.9; existing application data is not automatically migrated. Six-module baseline, full isolated integration (twice), manager race/vet, real-Docker regression and acknowledged-job persistence/retry checks across root-stack recreation passed. Five existing skips remain; no paid AI run or push. Next: M2.3.
- [x] M2.3 Package and pin a real non-interactive AI CLI in the selected worker image. Verify its installed authentication/instruction/sandbox behavior; keep the mock restricted to tests. Implementation: `b8fb6c0`; [CLI contract and host prerequisites](docs/WORKER_CLI.md). Selected `pagewright-worker:m2.3` pins Codex 0.153.4 and native package integrity; explicit Responses auth/instructions, stdin prompt, fresh state, bounded/redacted capture and pre-provider sandbox preflight replace the mock. User-approved narrow M2.6 prerequisite adds non-root UID 1000 and an audited default-deny namespace/mount seccomp delta, retaining zero capabilities and no-new-privileges. Installed CLI/fake-API tests prove shell execution, inside writes and denied outside writes/network/key inheritance; outer mounting stays denied. Package, service integration, manager/worker race/vet, real-Docker launch, production image, startup/recreation and actionlint passed. AppArmor profile parser-checked and CI opt-in provided; AppArmor enforcement and hosted CI not locally verified. Three existing skips remain; no paid calls or push. Next: M2.4.
- [x] M2.4 Package `pagewrightc` and a trusted versioned starter theme. After edits, validate allowed source changes, compile into a fresh output directory before replacing workspace `public/`, check HTML/assets and produce truthful manifest results. Implementation: `ef864e5`; [trusted build contract](docs/WORKER_BUILD.md). Selected `pagewright-worker:m2.4` contains root-owned read-only compiler 0.1.0 and starter 1.0.0. Snapshot/diff validation permits only content changes; digest-verified frozen source and fresh output feed the archive. Static HTML/local-reference and archive gates precede replacement/upload; manifest records actual paths, named checks and versions, explicitly marking browser checks unperformed. Package and full service integration suites, worker race/vet, manager config race/vet, installed compiler/permissions acceptance, installed CLI isolation regression, production image build and actionlint passed. Three existing skips remain; no browser/paid-provider/hosted-CI acceptance or push. Next: M2.5.
- [x] M2.5 Use latest completed draft → live → bootstrap as the default source selection. Expose that base to the user and verify two unpublished edits accumulate. Implementation: `cb9c35e`; [source-selection contract](docs/BUILD_SOURCE.md). Owner-scoped manifest-committed storage versions determine the newest non-bootstrap base, with deterministic timestamp/ID ordering and live/bootstrap fallbacks. Invalid/unavailable storage fails before dispatch; persisted submissions pin the original source across retries/restart/newer drafts/outages. Chat shows the actual accepted base. Two unpublished deterministic edits through the production worker/compiler preserve both edits in source/HTML, the immutable first draft and unchanged live pointer. Six-module package tests, full isolated service integration, gateway race/vet and UI contracts/zero-warning lint/build passed. Three existing skips remain; no paid-provider/browser/hosted-CI acceptance or push. Next: M2.6.
- [x] M2.6 Enforce worker CPU/memory/process/runtime/output limits and cancellation. Isolate writable source from theme/compiler; do not expose the Docker socket, unrelated host files or management credentials to AI execution. Implementation: `9fceb7c`; [isolation policy](docs/WORKER_ISOLATION.md), [host acceptance](docs/M2_6_HOST_ACCEPTANCE.md). Selected `pagewright-worker:m2.6` adds fixed resource/log/storage ceilings, read-only root, outer filesystem/PID isolation, detached-child teardown, job cancellation/watchdog and output/download bounds. User-approved proc compatibility preserves explicit AppArmor proc denies and non-proc masks; tool-only seccomp blocks namespace/mount bypasses after trusted setup. Compatible-image ID pinning and pre-provider denial checks fail closed. Package/service/daemon suites, local and enforcing-AppArmor host installed CLI/compiler acceptance, manager/worker race/vet, production image build and actionlint passed. Kill test restored; two serving skips remain. No privileged/unconfined mode, host-wide sysctl, paid calls, production deployment or push. Remote profiles remain runtime-loaded; persistent installation is an administrator step. Next: M2.7.
- [x] M2.7 Renew site locks, enforce fencing/attempt identity at result/artifact commit, and reject stale or duplicate terminal updates. Implementation: `9740d79`; [fenced commit contract and upgrade requirements](docs/FENCED_COMMITS.md). Selected `pagewright-worker:m2.7`. Atomic lease/fence acquisition, Redis-backed bounded renewal and non-resurrection of expired leases protect running attempts. Storage stages/syncs bytes, obtains an atomic attempt-bound digest reservation, then materializes the immutable write; bootstrap `initial` remains reserved. Results require live attempt identity and completion requires a manifest reservation. Terminal outcome, site/capacity release and lock removal are atomic; terminal duplicates return 409 while admission retries remain idempotent. Package suite, full race-enabled service integration, concurrent jobs beyond two original TTLs, streamed-upload expiry, manager/storage/worker race/vet, selected image build, installed CLI/compiler and real-Docker launch checks passed. Two serving skips remain. Drain/reconcile before coordinated manager/storage/worker upgrade; uncertain commit/result recovery remains M2.8/M2.9, authentication M4. No paid calls, production deployment or push. Next: M2.8.
- [x] M2.8 Retry result delivery with bounds; reconcile artifacts and worker exits when callbacks are lost. Record failed/spawn-error/timeout states and release locks on every terminal path. Implementation: `e0c484e`; [result and receipt recovery contract](docs/RESULT_RECOVERY.md). Selected `pagewright-worker:m2.8`. Four identical callback attempts within 25 seconds, bounded responses and authoritative job lookup resolve lost acknowledgements/terminal 409s without weakening fencing. Uncertain manifest/completion delivery does not send a contradictory failure. Bounded manager reconciliation verifies Docker ownership/attempt identity and exact artifact/log/manifest digest reservations; confirmed incomplete output fails conservatively with receipts retained. Redis atomically compares observed receipts/attempts and releases terminal reservations without reviving leases or deleting replacements; definite spawn failures now release their lease atomically too. Repository package suite, full race-enabled service integration, manager/worker race/vet, selected image build, installed CLI/compiler and real-Docker inspection/launch checks passed. Two existing serving skips remain. Storage outages retain capacity pending evidence; restart durability remains M2.9 and orphan cleanup/retention M2.10. No privileged containers, isolation relaxation, paid calls, remote changes, production deployment or push. Next: M2.9.
- [x] M2.9 Persist Redis data and durable job/version history; reconcile pending/running/uncertain jobs after restart, including a crash between gateway dispatch claim and manager receipt. Implementation: `25dcf08`; [durability, retention and operator runbook](docs/JOB_DURABILITY.md). Migration 009 adds bounded-per-job lifecycle history and durable recovery diagnostics. Gateway recovery claims ready submissions once, verifies manager outcomes and atomically updates history/submission/version; claimed-but-missing evidence is never redispatched. Production Redis startup enforces AOF/fsync/no-eviction settings and protects surviving legacy reservation TTLs without persisting leases. Thirty-day terminal dispatch bookkeeping cleanup preserves canonical jobs, request mappings, fences and digest receipts indefinitely; total identity storage is not bounded. Read-only packaged audit and conservative restore/quarantine policy cover missing/corrupt/legacy evidence without reopening terminal jobs. Repository packages, full race-enabled service integration, gateway/manager race/vet, production root image builds, independent-manager reconnect and isolated SIGKILL/recreation smoke passed. Two existing serving skips remain. Worker stays `pagewright-worker:m2.8`; no isolation changes, paid calls, remote changes, application-data migration, disk-loss/HA claim or push. Next: M2.10.
- [x] M2.10 Clean up exited/orphan containers and temporary data with a documented retention policy. Capture bounded, redacted logs correlated by site/job/version. Implementation: `ddbb163`; [retention and coordinated-upgrade policy](docs/WORKER_RETENTION.md). Selected `pagewright-worker:m2.10`. One-hour terminal grace, full Docker launch-identity verification, atomic terminal/reservation checks and seven-day allowlisted diagnostics precede non-force removal; lingering terminal workers require kill then fresh inspection. Missing/conflicting job evidence stays quarantined. Seven-day abandoned `.upload-*` cleanup coordinates with live writers using a stable cross-process lock; final objects and receipts remain untouched. New private diagnostics withhold arbitrary executor text and bound records to 4 KiB; immutable version logs and historical receipts remain retained, not retroactively scrubbed. Package suite, full race-enabled service integration twice, manager/storage/worker race/vet, selected worker build, installed CLI/compiler, real-Docker removal and isolated restart smoke passed. Two existing serving skips remain. Drain all storage writers for upgrade; unpublished staging bytes can expire, and quarantines/outages prevent a hard total-disk bound. No privileged containers, isolation relaxation, paid calls, remote cleanup/deployment or push. Next: M2.11.
- [x] M2.11 Repair skipped executor parsing/cancellation tests and test actual runner failures: invalid output, compiler error, upload failure, callback failure, timeout and restart. Implementation: `bb5f877`; [runner acceptance and scope](docs/RUNNER_ACCEPTANCE.md). Selected `pagewright-worker:m2.11`. Production-main subprocess tests use a deterministic CLI and real compiler to cover valid output, disallowed edits, invalid compiled references, compiler errors, artifact/log/manifest failures, bounded callback retries/lost acknowledgements, SIGTERM and SIGKILL before/after manifest. A caller-owned context tests deadline propagation without changing production limits. Fresh-manager Redis reconciliation covers retained complete/incomplete receipt evidence after restart, with no replay or terminal regression. M2.3/M2.6 executor tests remain enabled; corrected the stale skip inventory. Package suite, full race-enabled integration twice, final runner matrix three repeated runs without external networking, worker/manager race/vet, selected image build, installed CLI/compiler, real-Docker checks and isolated stack crash/restart smoke passed. Two unrelated serving skips remain. Acceptance is layered, not one paid-provider crash test. No isolation relaxation, privileged containers, paid calls, remote deployment/cleanup or push. Next: M2.12, requiring an explicitly cost-bounded provider run.
- [x] M2.12 Run one explicitly invoked, cost-bounded real-provider smoke test and verify the requested content changed in the resulting HTML. Implementation: `4f17a3c`; [procedure, budget policy and acceptance evidence](docs/PROVIDER_SMOKE.md). Selected `pagewright-worker:m2.12`; user-selected `gpt-5.6-luna`. Explicit model forwarding and a smoke-only gateway keep the real key out of workers, enforce three bounded requests with durable worst-case reservations, reject hosted tools and block uncertain charges. Real manager/worker/CLI/compiler/storage run changed only the requested source heading and verified compiled HTML while preserving initial bytes and isolation. Three successful API requests: 21,572 input and 209 output tokens; usage-derived token cost $0.00236171, including cache writes. Two prior Codex Mini attempts stopped on HTTP 404 without usage; all conservative reservations together were $1.8520048, below the authorized $2. No GPT-5.4 Mini paid call. Offline guard/full-stack checks, packages, race-enabled service integration, manager/worker race/vet, selected image build, installed CLI/compiler and real-Docker checks passed. Two unrelated serving skips remain. Temporary keys and disposable services were removed; `.env`, application data and sandbox restrictions unchanged. No remote deployment or push. All M2 items are complete; next M3.1, owner-scoped history and browser recovery.

## M3 — Browser workflow, preview and publication

Depends on M2. Exit: create → edit → refresh → preview → publish → edit → rollback works through the UI.

- [x] M3.1 Add owner-checked gateway job retrieval and persistent build history; update version state from manager results. Implementation: `38e908e`; [history API and recovery contract](docs/JOB_HISTORY_API.md). Owner/site-scoped single-job and paginated durable history reads survive missing manager/storage services without dispatching work. Public allowlisted fields/codes exclude private diagnostics and request identities. Site-keyed Chat history restores saved pending/running/completed/failed states on refresh, with pagination/manual retry and stale-response suppression. Version listings require both committed storage and completed durable outcomes for known submissions; late artifacts never revive failures. M2.9 remains the verified background reconciliation writer. Six-module package baseline, full race-enabled isolated service integration, gateway race/vet and UI contracts/lint/build passed; fresh-handler HTTP/SQL ownership, lifecycle reloads, pagination and deterministic worker reconciliation covered. Two existing serving skips remain. No paid calls, remote changes, application-data migration or push. Actual browser acceptance remains M3.12; automatic polling is next, M3.2.
- [x] M3.2 Poll active jobs from the UI with bounded backoff and consistent pending/running/completed/failed handling. Filter by current site/job and retain useful failure details. Implementation: `b1af7d6`; [polling contract](docs/JOB_HISTORY_API.md). Current-page pending/running jobs use owner-checked retrieval, exact site/job/source/target filtering and monotonic updates. Sequential 2/4/8/15-second backoff, 10-second request timeouts, 60-round/15-minute limits and five-consecutive-error pause prevent unbounded checks; 401/403/404 pause immediately. Cleanup aborts requests/timers and ignores late responses. Saved failures/codes survive outages, with safe explanations and job IDs; completion refreshes versions without a polling loop. Site-keyed Chat isolates navigation and submissions reset history to page one. Unscoped socket messages no longer alter Chat; removing the unused connection remains M3.3. Deterministic lifecycle/backoff/error/identity/cleanup tests, UI contracts, zero-warning lint, type checking and production build passed. No backend changes, paid calls, remote deployment or push; full browser acceptance remains M3.12. Next: M3.3.
- [x] M3.3 Disable the broken WebSocket integration for the polling MVP. Before re-enabling it, implement browser-compatible auth, origin checks, owner filtering, event delivery and stable reconnect cleanup. Implementation: `7f99456`; [retirement and re-enable gate](docs/JOB_HISTORY_API.md). Removed browser hook/reconnect/query-token transport, socket URL configuration, gateway upgrader/hub/pumps and unused socket dependency. `/ws` explicitly returns public, no-store HTTP 501 without inspecting or echoing credentials; generic OPTIONS preflight remains 200. Owner-checked polling is unchanged. Gateway handshake-denial tests, UI source/config guards and served-bundle/endpoint checks before and after root-stack recreation passed, alongside six-module Go baseline, gateway race/vet, UI contracts/polling tests, zero-warning lint/build and script checks. Two existing serving skips remain. Private `.env` and application data untouched; no paid calls, remote deployment or push. Removed code remains recoverable in Git history; no re-enable flag. Next: M3.4.
- [x] M3.4 Make Preview call the deployment API before opening a returned preview URL. Provision host routing on first preview, including before first publish. Implementation: `e22362d`; [activation contract and boundaries](docs/PREVIEW_ACTIVATION.md). Preview stages/activates the selected artifact and provisions missing nginx routing without replacing existing aliases/disabled policy. UI validates returned target/version/URL, guards pending actions, opens only after confirmation and retains a popup fallback link; failures show retry guidance without a success URL. Narrow M3.6/M3.7 prerequisites brought forward: configurable deployment URL scheme/port, nil-as-unchanged live/preview updates and checked DB write errors. Real worker/compiler/storage/nginx integration proves first preview before publication (live 404), then live/preview pointer independence. Two full race-enabled integration runs, six-module packages, gateway/serving race/vet and UI contracts/zero-warning lint/build passed; owner denial, operation ordering, failure responses, URL validation, modal closure and config preservation covered. Two existing serving skips remain. Production root Compose reload coordination remains M3.5; preview assets/all UI URLs M3.6; reconciliation/atomic changes M3.7/M3.8; browser acceptance M3.12. No paid calls, remote deployment, application-data changes or push. Next: M3.5.
- [x] M3.5 Implement the chosen serving/nginx lifecycle in Compose: supervised processes, configuration validation, controlled reload, rollback on reload failure and restart recovery. Implementation: `2e1eea2`; [topology, transaction and upgrade runbook](docs/HOSTING_LIFECYCLE.md). Tini/Go supervisor owns serving API and hosting nginx; either child exit tears down the pair for Compose restart. Existing public nginx becomes a fixed Host-preserving, DNS-refreshing proxy. Exclusive writer lock, serialized synced config journals/atomic replacement, nginx validation and loopback generation acknowledgment gate success; failed changes restore/reload prior config, uncertain rollback blocks writes, and startup recovers interrupted transactions before validation. Routing is confirmed before artifact pointer changes. Removed local no-op reload override; no Docker socket/privileged/PID-sharing access added to serving. Six-module baseline, serving race/vet, full race-enabled integration using production topology, real-nginx invalid/no-op reload rollback tests, root proxy/nginx-crash/recreation smoke and script/Compose checks passed. First smoke exposed Node fetch ignoring Host; corrected client and subsequent runs passed. Two existing serving skips remain; UI code unchanged (production image rebuilt). Application volumes/private `.env` untouched; disposable test resources removed, no paid calls, remote deployment or push. Coordinated serving/proxy upgrade required; atomic artifact switching and DB reconciliation remain M3.7/M3.8, browser acceptance M3.12. Next: M3.6.
- [x] M3.6 Return configured live/preview URLs; remove hard-coded HTTPS/port assumptions and unsupported `/preview/{build_id}` links. Implementation: `830c476`; [hosting contract and upgrade guide](docs/PREVIEW_ACTIVATION.md). Site create/list/detail and deployment responses share configured scheme/port URL generation. Dashboard/Chat use validated server URLs, disable unavailable destinations, and Chat refreshes links after deployment. Preview uses `preview.<site-fqdn>/`, with root-relative/relative links, nested pages, theme/page assets and relative directory redirects retaining the external origin. Live aliases remain live-only; reserved preview names and existing nginx collisions are rejected. Exact generated legacy configs migrate on preview activation with aliases/disabled policy preserved; custom configs require operator review. Real compiler/storage/production-proxy integration verifies both hosts, first preview with live 404, private/missing paths, second-preview isolation and promotion of all exact artifact bytes without archive changes. Six-module Go baseline, gateway/serving race/vet, full race-enabled integration, UI contracts/zero-warning lint/build, root hosting/crash/recreation smoke and whitespace/script checks passed. Fixture compile/asset-path/manifest expectations were corrected during verification; root smoke now bounds convergence on fresh connections while old nginx workers drain. Two existing serving skips remain. DNS/TLS for both hosts and coordinated gateway/serving/UI upgrade are required; existing configs migrate only on activation. No paid calls, remote deployment, application-data changes or push; disposable test resources removed. Next: M3.7; atomic artifact switching M3.8 and rendered-browser acceptance M3.12 remain open.
- [x] M3.7 Preserve the opposite live/preview DB pointer on updates and handle DB write errors. Reconcile DB and serving state if deployment only partially succeeds. Implementation: `ec47934`; [deployment protocol, recovery and upgrade runbook](docs/DEPLOYMENT_RECOVERY.md). Migration 010 retains one sequenced intent per site; site-row reservation admits one unresolved operation across both targets/gateway instances, reuses its identity on retry, and returns 409 for competing selections. Serving persists private pending/activating/terminal receipts, serializes operations, fences stale/conflicting requests and verifies completed pointers without reactivation. Gateway validates exact receipts and atomically commits intent status plus only the selected DB pointer; startup/bounded-batch background recovery replays persisted identities after uncertainty. Owner-only no-store deployment status endpoint added. Legacy writes/deletion cannot bypass enrolled receipts; enrolled-site deletion is blocked and unsafe automatic retention is omitted pending M3.8/M3.9. Six-module baseline, gateway/serving race/vet, full race-enabled integration and root startup/crash/recreation smoke passed. Tests cover duplicate/concurrent requests, stale completions, real PostgreSQL write failure/rollback, restart-style reconciliation, interrupted activation/outage, corrupt receipts, owner denial and real serving acknowledgment loss after actual compiled-artifact activation. Existing nginx drain-window acceptance now uses bounded public-resource convergence with exact-byte checks. Initial test setup/migration expectations corrected; two existing serving skips remain. Upgrade gateway/serving together and preserve DB sequence/receipts; atomic symlink switching, active-version retention and coordinated deletion remain unfinished. UI unchanged; no paid calls, remote deployment, application-data changes or push; disposable stacks removed. Next: M3.8.
- [x] M3.8 Replace remove-then-create symlinks with atomic replacement; stage extraction before activation. Prevent retention/deletion from removing active versions and test rollback. Implementation: `4466271`; [activation, retention and upgrade guide](docs/ATOMIC_ACTIVATION.md). Validated/synced staging precedes publication; a temporary same-filesystem symlink is atomically renamed over the selected pointer and its directory synced. Pre-rename failures preserve old output; post-rename uncertainty stays under M3.7 receipt recovery. Shared writer locking serializes activation/retention/deletion. Retention protects exact live/preview and receipt versions plus a one-minute grace period; the configured limit is a soft total serving-cache budget, with deterministic ordering and fail-closed handling of malformed evidence/path indirection. Fenced deployments now run safe cache cleanup; canonical storage/history remain intact for rollback after eviction. Whole-site removal checks active output/receipts before touching nginx routing; enrolled-site deletion remains blocked pending a coordinated tombstone protocol. Replaced the skipped cleanup test with deterministic assertions. Six-module baseline, final serving race/vet, full race-enabled integration and production startup/crash/recreation smoke passed. Tests cover continuous readers, forced activation/cleanup overlap, rename/post-rename failures, rollback after eviction, independent preview, exact pins, grace/ties, staging preservation, symlink rejection and routing preservation on refused deletion. One pre-existing skip remains (`TestLoadConfigDefaults`). No schema/UI changes, paid calls, remote deployment, application-data changes or push; disposable test stacks removed. Atomic pointer selection is not a multi-request browser snapshot; canonical storage is required for evicted rollback. Next: M3.9.
- [ ] M3.9 Hide unsupported attachments, arbitrary domains/aliases and OAuth controls; disable unsupported backend actions in MVP mode. Keep the supported text-only request path explicit. Hide/disable site deletion where M3.7's deployment-record guard currently returns 409, until coordinated deletion exists.
- [ ] M3.10 Preserve drafts across session expiry/re-authentication; show progress, retryable failures, empty states and meaningful version labels. Refresh dashboard deployment state after actions.
- [ ] M3.11 Verify keyboard access, focus handling and usable dashboard/chat/modals on mobile and desktop.
- [ ] M3.12 Add browser acceptance coverage for the full journey, two sequential edits, preview/live independence, refresh, build failure and rollback. No manual HTML seeding or DB edits in this suite.

## M4 — Remote pilot release gate

Apply relevant protections while building M1/M2; all items below block admitting remote testers.

- [ ] M4.1 Require invitations or operator provisioning; implement per-user/site build quotas, request throttling and bounded AI spending/concurrency.
- [ ] M4.2 Remove default production secrets and fail startup for missing critical configuration. Wire PostgreSQL credentials consistently; redact reset tokens, credentials and private prompts from routine logs.
- [ ] M4.3 Restrict internal service/database/Redis ports; authenticate service writes and scoped worker callbacks. Test direct unauthenticated requests and cross-job credential misuse.
- [ ] M4.4 Validate/normalize platform subdomains, reserved names, site/version identifiers and path containment before filesystem or nginx use. Reject configuration injection and traversal.
- [ ] M4.5 Bound JSON bodies, archive bytes, decompressed size and file count; reject unsafe links/file types. Test compiler/assets/archive escape attempts and malformed metadata.
- [ ] M4.6 Replace wildcard HTTP/socket origin policies with configuration and tests. Keep application and generated-site origins separate; configure appropriate security headers.
- [ ] M4.7 Test cross-user access for site, job, version, download, preview/deploy and deletion endpoints; apply owner filtering before any event delivery.
- [ ] M4.8 Complete password-reset email delivery and single-use token consumption, expiry and throttling; normalize auth responses and apply a consistent password policy. Confirm session expiry/recovery preserves ongoing work.
- [ ] M4.9 Configure DNS and HTTPS for app, live and preview hosts; document local equivalents and disable unsupported domain aliases.
- [ ] M4.10 Add downstream request cancellation, appropriate server/client timeouts, DB pool limits and dependency readiness checks.
- [ ] M4.11 Document and exercise backups/restores of PostgreSQL, artifacts and deployment metadata in disposable infrastructure; verify recovery of the published site.
- [ ] M4.12 Resolve or replace skipped serving cleanup/config tests; run meaningful race checks on job/lock/conversation state and any enabled WebSocket hub.
- [ ] M4.13 Run all release scenarios in PLAN.md, including worker/manager restart, failed deployment, two-user isolation and real AI smoke. Record commands, results, limits and known issues.
- [ ] M4.14 Publish operator runbooks for failed jobs, credential rotation, disk pressure, backup restore and rollback; update README to the verified release state.

## Deferred backlog — not MVP blockers

Retained and grouped from the previous roadmap; revisit after pilot feedback.

- [ ] Compiler/themes: more themes, theme validation tooling, richer props validation, incremental builds, drafts, sitemap/RSS, SEO suggestions and marketplace.
- [ ] Editing UX: safe attachments, browser screenshots/preview checks, cloning, team collaboration, analytics and localization.
- [ ] Identity: Google/other OAuth, email verification for open signup, refresh sessions, 2FA and richer account administration.
- [ ] Domains: customer-domain ownership verification, alias lifecycle, automatic certificates, CDN integration and cache invalidation.
- [ ] API: search/filter/pagination extensions, bulk operations, versioning, OpenAPI, webhooks and external integrations; GraphQL only if a concrete need emerges.
- [ ] Orchestration: Kubernetes/Helm, priority queues, dead-letter tooling, job cancellation UI, pre-warmed pools and autoscaling; NATS/Kafka/RabbitMQ backends.
- [ ] Storage: S3-compatible/Azure/GCS/RustFS adapters, retention automation, deduplication, compression options, metadata search, multipart/resumable uploads, ETags and caching.
- [ ] Operations: metrics/Grafana, alerting, tracing/error tracking, load testing, container/dependency scanning, IaC, mTLS/secret-management tooling and multi-region recovery.
- [ ] Platform: billing, white-labeling, plugins/template extensions and expanded developer/operator documentation.

The old fixed two-week schedule and coverage-percentage targets are superseded by milestone acceptance checks. Features should move into the MVP only when necessary to pass those checks or in response to explicit product scope changes.
