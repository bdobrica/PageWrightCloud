# Disposable acceptance only. Never combine this overlay with application data.
services:
  postgres:
    ports: ["127.0.0.1::5432"]
  redis:
    ports: ["127.0.0.1::6379"]
  manager:
    ports: ["127.0.0.1::8081"]
  storage:
    ports: ["127.0.0.1::8080"]
  serving:
    ports: ["127.0.0.1::8083"]
  themes:
    ports: ["127.0.0.1::80"]
